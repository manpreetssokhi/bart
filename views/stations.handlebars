<div class="container">
    <h1>Stations List</h1>
    <div>
        <ul>
            {{#each stations}}
            <li>
                <span style="color: #1FA2FF"> {{ this.name }} </span> 
                <span style="color: #E3000B"> {{ this.abbr }} </span>
            </li>
            {{/each}}
        </ul>
    </div>
</div>